<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="22dp"
    android:background="@color/pageBg">

    <TextView
        android:id="@+id/tvQrEyebrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EVENT CREATED"
        android:textSize="9sp"
        android:letterSpacing="0.18"
        android:textAllCaps="true"
        android:textColor="@color/textMuted" />

    <TextView
        android:id="@+id/tvEventNameDisplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Event Name"
        android:textSize="24sp"
        android:fontFamily="sans-serif-medium"
        android:layout_marginBottom="24dp" />

    <!-- Shown for public events only -->
    <ImageView
        android:id="@+id/ivQrCode"
        android:layout_width="240dp"
        android:layout_height="240dp"
        android:background="@drawable/bg_input_field"
        android:layout_marginBottom="24dp" />

    <!-- Shown for public events only -->
    <TextView
        android:id="@+id/tvQrHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Scan to view event details"
        android:textColor="@color/textSecondary"
        android:layout_marginBottom="24dp" />

    <!-- US 02.01.02: Shown for private events instead of QR code -->
    <TextView
        android:id="@+id/tvPrivateNotice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No QR code generated.\nPrivate events are invite-only and\ndo not have a promotional QR code."
        android:textColor="@color/textSecondary"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_marginBottom="24dp"
        android:visibility="gone" />

    <!-- Shown for public events only -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnShare"
        style="@style/Widget.MaterialComponents.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="SHARE QR CODE"
        android:textColor="@color/primaryAccent"
        android:textSize="12sp"
        android:letterSpacing="0.06"
        app:cornerRadius="100dp" />

</LinearLayout>